/**
 * 개발 DB 시드 사용자의 비밀번호. `next dev` 의 로그인 화면에만 안내로 보인다.
 * 시드는 운영에서 돌지 않는다(prisma/seed.ts 가 막는다).
 */
export const DEV_SEED_PASSWORD = "open-todo-dev";
export const DEV_SEED_EMAIL = "hong@example.com";
